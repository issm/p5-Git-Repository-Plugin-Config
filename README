This is Perl module Git::Repository::Plugin::Config.

INSTALLATION

Git::Repository::Plugin::Config installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan Git::Repository::Plugin::Config

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

Git::Repository::Plugin::Config documentation is available as in POD. So you can do:

    % perldoc Git::Repository::Plugin::Config

to read the documentation online with your favorite pager.

issm
